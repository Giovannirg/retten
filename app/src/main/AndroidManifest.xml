<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.retten.retten">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.search"
            android:label="@string/title_activity_search"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activities.seller_page" />
        <activity android:name=".Activities.cart_item" />
        <activity android:name=".Activities.ShoppingCartWindow" />
        <activity android:name=".Activities.IndividualProduct" />
        <activity android:name=".Activities.IndividualProductSeller" />
        <activity android:name=".Activities.CheckOutScreen" />
        <activity android:name=".Activities.AddProductForm" />
        <activity android:name=".Activities.forgotPassword" />
        <activity android:name=".Activities.AdminPage" />
        <activity android:name=".Activities.AddSupermarktActivity"></activity>
    </application>

</manifest>